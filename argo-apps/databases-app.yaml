apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: uit-go-databases
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/UIT-GO/gitops-repo
    targetRevision: main
    path: databases
  destination:
    server: https://kubernetes.default.svc
    namespace: databases
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true