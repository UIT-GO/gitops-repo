apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: uit-go-messaging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/UIT-GO/gitops-repo
    targetRevision: main
    path: messaging
  destination:
    server: https://kubernetes.default.svc
    namespace: messaging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true